extends layout

block content
  .inner
    h2= title
    .map
      .autocomplete
      input.autocomplete__input(type='text' placeholder="Search for anything" name="geolocation")
      #map
        p loading map